node_group::config:
 # allow the use of a value in another node_group
 allow_duplicates: False
 # allow referencing other node_groups, will expand and merge values to base type
 allow_inception: False

development:
 - "dev-host1"
 - "dev-host2"

preproduction:
 - "pre-host1"
 - "pre-host2"

uat:
 - "uat-host1"

production:
 - "host1"
 - "host2"
 - "host3a"
 - "host3b"