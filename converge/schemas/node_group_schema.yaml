# Node Group yaml schema for validation
type: map
mapping:
  node_group::config:
    type: map
    mapping:
      allow_duplicates:
        type: bool
        required: True
      allow_inception:
        type: bool
        required: True
  regex;(^((?!node_group::config).)*$):
    type: seq
    sequence:
      - type: str


